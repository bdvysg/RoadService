using System;
using System.Collections.Generic;
using System.DirectoryServices;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace RoadService.Classes
{
    public class Register
    {/*
        
        Task t;
        Tariff tf;
        AddressList adr;
        EmployeeTimeTable empTT;
        ResourceTimeTable resTT;

        public Register(Tariff tf, AddressList adr, EmployeeTimeTable empTT, ResourceTimeTable resTT)
        {
            this.tf = tf;
            this.adr = adr;
            this.empTT = empTT;
            this.resTT = resTT;
        }

        public void newTask()
        {
            t = new Task();
        }

        public bool isValidAddress(string address)
        {
            t.IsValidAddress(address);
        }

        public void GetResourcesAndEmployees()
        {
            t.GetResources();
            t.GetEmployees();
        }

        public void reserveResourcesAndEmployees(List<Resource> resources, List<Employee> employees)
        {
            t.reserveResources(resources, employees);
            t.reserveEmployees(employees);
        }

        public decimal getPrice(Tariff tf)
        {
            t.getPrice(tf);
        }
        
    */}
}
